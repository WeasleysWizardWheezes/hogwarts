name: Setup Node.js
description: Setup Node.js with npm cache and GitHub Packages registry auth

runs:
  using: composite
  steps:
    - uses: actions/setup-node@v4
      with:
        node-version: 20
        registry-url: 'https://npm.pkg.github.com'
        scope: '@${{ github.repository_owner }}'