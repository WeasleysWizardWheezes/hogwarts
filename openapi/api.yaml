openapi: 3.0.3
info:
  title: Hogwarts API Dummy
  version: 0.1.0-SNAPSHOT-test3
paths:
  /dummy:
    get:
      summary: Dummy endpoint to trigger generation. Remove in the future.
      responses:
        '200':
          description: Dummy OK
  /test:
    get:
      summary: Dummy endpoint to trigger test pipeline jobs. Remove in the future.
      responses:
        '200':
          description: Test 3 OK