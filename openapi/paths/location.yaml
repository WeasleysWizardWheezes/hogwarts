paths:
  /api/v1/locations:
    get:
      tags: [Location]
      summary: Alle Standorte auflisten
      operationId: listLocations
      parameters:
        - $ref: '../components/parameters/common.yaml#/PageParam'
        - $ref: '../components/parameters/common.yaml#/SizeParam'
      responses:
        '200':
          description: Liste aller Standorte
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '../components/schemas/location.yaml#/LocationResponse'
                  page:
                    $ref: '../components/schemas/common.yaml#/PaginationMeta'
    post:
      tags: [Location]
      summary: Neuen Standort anlegen
      operationId: createLocation
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '../components/schemas/location.yaml#/CreateLocationRequest'
      responses:
        '201':
          description: Standort erstellt
          headers:
            Location:
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '../components/schemas/location.yaml#/LocationResponse'
        '400':
          $ref: '../components/responses/errors.yaml#/BadRequest'
        '409':
          $ref: '../components/responses/errors.yaml#/Conflict'

  /api/v1/locations/{locationId}:
    get:
      tags: [Location]
      summary: Standort anhand der ID laden
      operationId: getLocation
      parameters:
        - name: locationId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Standort gefunden
          content:
            application/json:
              schema:
                $ref: '../components/schemas/location.yaml#/LocationResponse'
        '404':
          $ref: '../components/responses/errors.yaml#/NotFound'
    put:
      tags: [Location]
      summary: Standort aktualisieren
      operationId: updateLocation
      parameters:
        - name: locationId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '../components/schemas/location.yaml#/UpdateLocationRequest'
      responses:
        '200':
          description: Standort aktualisiert
          content:
            application/json:
              schema:
                $ref: '../components/schemas/location.yaml#/LocationResponse'
        '400':
          $ref: '../components/responses/errors.yaml#/BadRequest'
        '404':
          $ref: '../components/responses/errors.yaml#/NotFound'
    delete:
      tags: [Location]
      summary: Standort löschen
      operationId: deleteLocation
      parameters:
        - name: locationId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '204':
          description: Standort gelöscht
        '404':
          $ref: '../components/responses/errors.yaml#/NotFound'
        '409':
          $ref: '../components/responses/errors.yaml#/Conflict'

  /api/v1/members/{memberId}/location:
    put:
      tags: [Location]
      summary: Mitglied einem Standort zuweisen
      operationId: assignMemberToLocation
      parameters:
        - name: memberId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '../components/schemas/location.yaml#/AssignMemberToLocationRequest'
      responses:
        '200':
          description: Mitglied Standort zugewiesen
          content:
            application/json:
              schema:
                $ref: '../components/schemas/location.yaml#/MemberLocationResponse'
        '400':
          $ref: '../components/responses/errors.yaml#/BadRequest'
        '404':
          $ref: '../components/responses/errors.yaml#/NotFound'