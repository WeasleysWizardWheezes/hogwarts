paths:
  /api/v1/vehicles:
    get:
      tags: [Vehicles]
      summary: Alle Fahrzeuge auflisten
      operationId: listVehicles
      parameters:
        - $ref: '../components/parameters/common.yaml#/PageParam'
        - $ref: '../components/parameters/common.yaml#/SizeParam'
        - name: vehicleGroupId
          in: query
          required: false
          description: Filter nach Fahrzeuggruppe
          schema:
            type: string
            format: uuid
        - name: status
          in: query
          required: false
          description: Filter nach Status
          schema:
            $ref: '../components/schemas/vehicle.yaml#/VehicleStatus'
      responses:
        '200':
          description: Liste aller Fahrzeuge
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '../components/schemas/vehicle.yaml#/VehicleResponse'
                  page:
                    $ref: '../components/schemas/common.yaml#/PaginationMeta'
        '400':
          $ref: '../components/responses/errors.yaml#/BadRequest'
        '401':
          $ref: '../components/responses/errors.yaml#/Unauthorized'

    post:
      tags: [Vehicles]
      summary: Neues Fahrzeug anlegen
      operationId: createVehicle
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '../components/schemas/vehicle.yaml#/CreateVehicleRequest'
      responses:
        '201':
          description: Fahrzeug erstellt
          headers:
            Location:
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '../components/schemas/vehicle.yaml#/VehicleResponse'
        '400':
          $ref: '../components/responses/errors.yaml#/BadRequest'
        '401':
          $ref: '../components/responses/errors.yaml#/Unauthorized'
        '409':
          $ref: '../components/responses/errors.yaml#/Conflict'

  /api/v1/vehicles/{vehicleId}:
    get:
      tags: [Vehicles]
      summary: Fahrzeug anhand der ID laden
      operationId: getVehicle
      parameters:
        - name: vehicleId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Fahrzeug gefunden
          content:
            application/json:
              schema:
                $ref: '../components/schemas/vehicle.yaml#/VehicleResponse'
        '401':
          $ref: '../components/responses/errors.yaml#/Unauthorized'
        '404':
          $ref: '../components/responses/errors.yaml#/NotFound'

    put:
      tags: [Vehicles]
      summary: Fahrzeug vollständig aktualisieren
      operationId: updateVehicle
      parameters:
        - name: vehicleId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '../components/schemas/vehicle.yaml#/UpdateVehicleRequest'
      responses:
        '200':
          description: Fahrzeug aktualisiert
          content:
            application/json:
              schema:
                $ref: '../components/schemas/vehicle.yaml#/VehicleResponse'
        '400':
          $ref: '../components/responses/errors.yaml#/BadRequest'
        '401':
          $ref: '../components/responses/errors.yaml#/Unauthorized'
        '404':
          $ref: '../components/responses/errors.yaml#/NotFound'
        '409':
          $ref: '../components/responses/errors.yaml#/Conflict'

    patch:
      tags: [Vehicles]
      summary: Fahrzeug teilweise aktualisieren
      operationId: patchVehicle
      parameters:
        - name: vehicleId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '../components/schemas/vehicle.yaml#/UpdateVehicleRequest'
      responses:
        '200':
          description: Fahrzeug aktualisiert
          content:
            application/json:
              schema:
                $ref: '../components/schemas/vehicle.yaml#/VehicleResponse'
        '400':
          $ref: '../components/responses/errors.yaml#/BadRequest'
        '401':
          $ref: '../components/responses/errors.yaml#/Unauthorized'
        '404':
          $ref: '../components/responses/errors.yaml#/NotFound'
        '409':
          $ref: '../components/responses/errors.yaml#/Conflict'

    delete:
      tags: [Vehicles]
      summary: Fahrzeug archivieren (Soft Delete)
      operationId: archiveVehicle
      description: Archiviert das Fahrzeug. Diese Operation ist ein Soft Delete - die Daten bleiben erhalten, sind aber als archiviert markiert. Ein Fahrzeug kann nur archiviert werden, wenn es nicht im Einsatz ist.
      parameters:
        - name: vehicleId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '204':
          description: Fahrzeug archiviert
        '401':
          $ref: '../components/responses/errors.yaml#/Unauthorized'
        '404':
          $ref: '../components/responses/errors.yaml#/NotFound'
        '409':
          $ref: '../components/responses/errors.yaml#/Conflict'
